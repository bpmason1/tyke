package main;

type struct { height: int, width: int, depth: int } Rect ;

def main() -> int {
  let mut rect = Rect{ height: 4, width: 9, depth: 3 };
  return returnCalcResult(4, 9) ;
}

def volume(rect: Rect) -> int {
  let V = 1;
  return V;
}

def returnCalcResult(first : int, second : int) -> int {
  return (first + 6) + 2 *(second + one());
}

def one() -> int {
  let mut val = 2;
  val = 1;
  return val;
}
