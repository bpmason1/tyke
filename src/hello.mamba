package main;

type struct { height: int, width: int } Rect ;

def main() -> int {
  return returnCalcResult(4, 9) ;
}

def returnCalcResult(first : int, second : int) -> int {
  return (first + 6) + 2 *(second + one());
}

def one() -> int {
  let mut val = 2;
  val = 1;
  return val;
}
