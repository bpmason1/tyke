package main;

type struct { data: int } Number;
type struct { x: Number, y: Number } Point;
type struct { pt1: Point, pt2: Point } Line;


def main() -> int {
  let line = newLine(2, 5, 6, 18);

  let x = nested_arith_parens(4);
  print(x);
  return 0;
}

def nested_arith_parens(y: int) -> int {
  let x = (y + ((9 - 2*(1+2))*2)) + (3 + (1 + 1)) * ((7+1)/4);
  # x = 10 + 5 * 2
  return x;
}
